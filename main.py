import sys

import logging
import json
import git

import server.PeriodicFuzzer as pf

if __name__ == '__main__':
    logging.basicConfig(stream=sys.stdout, level=logging.DEBUG)
    logging.log(logging.INFO, "not implemented")
